$if(title)$
\title{$title$: $if(subtitle)$$subtitle$$endif$}
$endif$
\usepackage{etoolbox}
\makeatletter
\providecommand{\subtitle}[1]{% add subtitle to \maketitle
	\apptocmd{\@title}{\par {\Large #1 \par}}{}{}
}
\makeatother
\subtitle{BSc Thesis}
\author{
	$for(by-author)$%
	$by-author.name.literal$ \orcidlink{$by-author.orcid$}
	\\\medskip $by-author.email$%
	$for(by-author.affiliations)$%
	\\\medskip
	\\\medskip
	$if(it.department)$%
	Department of $it.department$
	\\\medskip
	$endif$%
	$it.name$
	$endfor$%
	$sep$ \and $endfor$%
}
\date{$date$}
